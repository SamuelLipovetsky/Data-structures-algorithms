#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <vector>
#include <set>
#include <queue>
#include <algorithm>
#include <bits/stdc++.h>

#include <stdio.h>
#include <stdlib.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int x, y, temp;
    //gredy solution
    char *buffer;
    size_t bufsize = 2 * 100000;

    buffer = (char *)malloc(2 * 100000 * sizeof(char));
    cin >> x;
    for (int i = 0; i < x; i++)
    {
        getline(&buffer, &bufsize, stdin);
        for (int j = 1; j < x; j++)
        {
            while (1)
            {
                for (int k = 0; k < ; k++)
                {
                    /* code */
                }
            }
        }
    }
}
